; Overrides for local development (no Redis in docker-compose.yml)
session.save_handler = files
session.save_path = "/tmp"

; Pick up PHP file changes without rebuilding the image
opcache.enable = 0
opcache.enable_cli = 0
